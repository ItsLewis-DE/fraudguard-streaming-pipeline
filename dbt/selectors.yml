selectors:
  - name: core_gate
    description: Canonical models, their upstream nodes and associated tests.
    definition:
      method: tag
      value: core
      parents: true

  - name: training_gate
    description: Training mart with upstream canonical and readiness checks.
    definition:
      method: tag
      value: training
      parents: true