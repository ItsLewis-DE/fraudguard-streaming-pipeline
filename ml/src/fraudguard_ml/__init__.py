"""FraudGuard's reproducible training-data and ML experiment package.

The package validates ClickHouse training contracts, creates immutable Parquet
snapshots with provenance manifests, and exposes operational checks through the
``fraudguard`` command-line interface.
"""

__version__ = "0.1.0"
